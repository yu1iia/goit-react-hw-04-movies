import React from 'react';

// Components
import Navigation from '../Navigation/Navigation';

const AppBar = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default AppBar;
